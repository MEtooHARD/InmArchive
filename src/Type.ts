export type ChildrenOnly = {
    children: React.ReactNode;
};

export type idetity_data_Discord = {
    avatar_url: string;
    custom_claims: {
        global_name: string
    };
    email: string;
    email_farified: boolean;
    full_name: string;
    iss: string;
    name: string;
    phone_verified: boolean;
    picture: string;
    provider_id: string;
    sub: string;
};