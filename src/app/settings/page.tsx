"use client"
import { useContext, useState } from "react";
import Switch from "@/components/switch/switch";
// import '../settings/switch.css';

export default function Page() {
  // const settings = useContext(Settings);
  // const [show, setShow] = useState(settings.showViewPort);

  return <>{"here's nothing"}</>
  {/* <div className="flex justify-center">
    <div className="flex flex-col items-center m-10 flex-wrap bg-slate-600 p-3 rounded-2xl">
      <Switch on={show} toggle={() => setShow(!show)} text="Show Window"></Switch>
    </div>
  </div> */};
};